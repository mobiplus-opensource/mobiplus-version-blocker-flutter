name: version_blocker_flutter
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.14.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  firebase: ^9.0.2
  firebase_core: ^1.10.5
  firebase_database: ^9.0.2
  flutter:
    sdk: flutter
  package_info: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0


flutter: